{% extends "base.html.twig" %}

{% block title %}Modifier{% endblock %}

{% block content %}
    <div class="container">
        <div class="row">
            <div class="col s12 m12 l12">
                <h4>Modifier l'evenement {{ evenement.title }}</h4>
                {% if evenement.picture is not null %}
                    <img width="300" src="{{ asset('uploads/' ~ evenement.picture) }}">
                {% endif %}
            </div>
        </div>
        {% include "include/article_form.html.twig" %}
    </div>
{% endblock %}